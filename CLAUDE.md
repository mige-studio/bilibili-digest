# B站精读项目入口

## 当前主线

本项目为独立 Chrome 扩展。首批以抖音精读和小红书精读的视频能力为参考，在电脑端公开 B 站普通视频页提供整段转写、说话人分组、时间点回听、画线精读、内容总结、笔记和本机资料保存。

## 当前阶段

当前处于首个本机候选版建设，不是已发布版本。先完成公开普通视频和当前分 P 的短样闭环，再做真实语音服务与用户听辨验收；自动检查、模拟结果和页面展示不能替代真实视频验收。

## 默认读取

先读 README.md；功能边界与验收读 docs/ACCEPTANCE.md。运行代码在 src/，检查在 tests/，发布包在 dist/。涉及阅读与语音链路时，只读参考 /Users/amila/程序工作台/douyin-digest 与 /Users/amila/程序工作台/xiaohongshu-digest，不修改参考项目。

## 数据与身份边界

- 一个资料对象由 BV 号与当前分 P 的 CID 共同确定；不把不同分 P 的说话人编号、逐字稿或笔记混在一起。
- 优先读取当前页面可用字幕；无可用字幕时才允许用户主动提交当前分 P 音轨转写。
- 说话人 1／2 是本期声音分组，不是人脸识别或跨视频固定身份；允许用户为本期编号填写姓名。
- API Key、浏览器资料、逐字稿和笔记只保存在用户扩展空间或用户选择的本机目录，不进入源码、文档、日志、安装包或 Git。

## 停止线

- 首批不处理直播、番剧／影视会员内容、充电专属、私密／受限、互动视频、移动端和整套合集批量转写。
- 不导出浏览器 Cookie，不读取其他扩展存储，不批量采集站内内容，不把临时音轨保存成下载工具。
- 每个分 P 单独确认和提交，避免意外产生整套合集费用。
- 不把源码完成写成浏览器已加载，不把模拟转写写成真人说话人准确；交付前必须在真实页面检查可见界面和主要操作。
- 发布、GitHub、公司 Skill 工作台和正式安装包是后续独立步骤；未获明确授权前不执行。

## 发布与上线

正式发布使用 Codex 全局 Skill `internal-tool-standard-publish`。当前尚未建立 `deploy/release-profile.json`，发布前须先完成新程序接入，不从其他项目或旧脚本猜测发布入口。
